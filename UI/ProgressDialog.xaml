<Window x:Class="StoryManager.UI.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="StoryManager" WindowStartupLocation="CenterOwner"
        Width="440" SizeToContent="Height" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True"
        Closing="Window_Closing">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeaderText" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8" d:Text="Loading stories..." />

        <ProgressBar Grid.Row="1" x:Name="Bar" Height="20" Minimum="0" Maximum="100" />

        <TextBlock Grid.Row="2" x:Name="StatusText" Margin="0,8,0,0" TextWrapping="Wrap" d:Text="0 of 0" />

        <Button Grid.Row="3" x:Name="OkButton" Content="OK" Width="80" Margin="0,12,0,0"
                HorizontalAlignment="Right" Visibility="Collapsed" IsDefault="True" Click="OkButton_Click" />
    </Grid>
</Window>
